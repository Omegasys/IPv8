[package]
name = "ipv8-transport"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

ipv8-core = { path = "../core" }
ipv8-crypto = { path = "../crypto" }
