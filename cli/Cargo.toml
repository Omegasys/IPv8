[package]
name = "ipv8-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
ipv8-core = { path = "../core" }
ipv8-network = { path = "../network" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
clap = { version = "4.0", features = ["derive"] }
